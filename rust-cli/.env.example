# Override values from config/default.toml using APP__<KEY> (double underscore separator).
# Example: change the greeting prefix
# APP__GREETING=Hi

# Override log level (also honored by RUST_LOG environment variable)
# APP__LOG_LEVEL=debug

# RUST_LOG takes priority over APP__LOG_LEVEL when set
# RUST_LOG=info
