name: Scaffold Verify go-grpc-api

on:
  push:
    branches: [main]
    paths:
      - 'go-grpc-api/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-go-grpc-api.yml'
      - '.github/workflows/scaffold-verify-go.yml'
  pull_request:
    paths:
      - 'go-grpc-api/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-go-grpc-api.yml'
      - '.github/workflows/scaffold-verify-go.yml'

jobs:
  scaffold:
    uses: ./.github/workflows/scaffold-verify-go.yml
    with:
      template: go-grpc-api
