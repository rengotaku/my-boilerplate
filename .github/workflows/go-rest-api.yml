name: go-rest-api CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/go-rest-api/**'
      - '.github/workflows/go-rest-api.yml'
      - '.github/workflows/go-ci.yml'
  pull_request:
    paths:
      - 'boilerplates/go-rest-api/**'
      - '.github/workflows/go-rest-api.yml'
      - '.github/workflows/go-ci.yml'

jobs:
  ci:
    uses: ./.github/workflows/go-ci.yml
    with:
      working-directory: boilerplates/go-rest-api
      test-paths: "./internal/..."
