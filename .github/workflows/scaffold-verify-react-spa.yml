name: Scaffold Verify react-spa

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/react-spa/**'
      - 'boilerplates/shared-react-ui/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-react-spa.yml'
      - '.github/workflows/scaffold-verify-react.yml'
  pull_request:
    paths:
      - 'boilerplates/react-spa/**'
      - 'boilerplates/shared-react-ui/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-react-spa.yml'
      - '.github/workflows/scaffold-verify-react.yml'

jobs:
  scaffold:
    uses: ./.github/workflows/scaffold-verify-react.yml
    with:
      template: react-spa
