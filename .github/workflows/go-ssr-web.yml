name: go-ssr-web CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/go-ssr-web/**'
      - '.github/workflows/go-ssr-web.yml'
      - '.github/workflows/go-ci.yml'
  pull_request:
    paths:
      - 'boilerplates/go-ssr-web/**'
      - '.github/workflows/go-ssr-web.yml'
      - '.github/workflows/go-ci.yml'

jobs:
  ci:
    uses: ./.github/workflows/go-ci.yml
    with:
      working-directory: boilerplates/go-ssr-web
      test-paths: "./internal/..."
