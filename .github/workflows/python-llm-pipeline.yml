name: python-llm-pipeline CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/python-llm-pipeline/**'
      - '.github/workflows/python-llm-pipeline.yml'
  pull_request:
    paths:
      - 'boilerplates/python-llm-pipeline/**'
      - '.github/workflows/python-llm-pipeline.yml'

defaults:
  run:
    working-directory: boilerplates/python-llm-pipeline

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install $(cat .python-version)

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Lint
        run: uv run ruff check .

      - name: Type check
        run: uv run mypy src/

      - name: Test
        run: uv run pytest --cov=src/mypipeline --cov-report=term-missing
