name: Scaffold Verify go-ssr-web

on:
  push:
    branches: [main]
    paths:
      - 'go-ssr-web/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-go-ssr-web.yml'
      - '.github/workflows/scaffold-verify-go.yml'
  pull_request:
    paths:
      - 'go-ssr-web/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-go-ssr-web.yml'
      - '.github/workflows/scaffold-verify-go.yml'

jobs:
  scaffold:
    uses: ./.github/workflows/scaffold-verify-go.yml
    with:
      template: go-ssr-web
