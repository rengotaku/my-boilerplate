name: static-lp CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/static-lp/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/static-lp.yml'
  pull_request:
    paths:
      - 'boilerplates/static-lp/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/static-lp.yml'

defaults:
  run:
    working-directory: boilerplates/static-lp

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: boilerplates/static-lp/.node-version
          cache: 'npm'
          cache-dependency-path: boilerplates/static-lp/package-lock.json

      - name: Compose shared-react-ui
        run: make compose-ui

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Type check
        run: npx tsc --noEmit

      - name: Test with coverage
        run: |
          npm run test:coverage
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          echo "Coverage: $COVERAGE%"
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "Coverage $COVERAGE% is below 80%"
            exit 1
          fi

      - name: Build (prerender + verify-prerender included)
        run: npm run build
