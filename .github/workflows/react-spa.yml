name: react-spa CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/react-spa/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/react-spa.yml'
  pull_request:
    paths:
      - 'boilerplates/react-spa/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/react-spa.yml'

defaults:
  run:
    working-directory: boilerplates/react-spa

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: boilerplates/react-spa/.node-version
          cache: 'npm'
          cache-dependency-path: boilerplates/react-spa/package-lock.json

      - name: Compose shared-react-ui
        run: make compose-ui

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Type check
        run: npx tsc --noEmit

      - name: Test with coverage
        run: |
          npm run test:coverage
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          echo "Coverage: $COVERAGE%"
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "Coverage $COVERAGE% is below 80%"
            exit 1
          fi

      - name: Build
        run: npm run build
