name: Scaffold Verify react-spa-graphql

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/react-spa-graphql/**'
      - 'boilerplates/shared-react-ui/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-react-spa-graphql.yml'
      - '.github/workflows/scaffold-verify-react.yml'
  pull_request:
    paths:
      - 'boilerplates/react-spa-graphql/**'
      - 'boilerplates/shared-react-ui/**'
      - 'scripts/scaffold/**'
      - '.github/workflows/scaffold-verify-react-spa-graphql.yml'
      - '.github/workflows/scaffold-verify-react.yml'

jobs:
  scaffold:
    uses: ./.github/workflows/scaffold-verify-react.yml
    with:
      template: react-spa-graphql
