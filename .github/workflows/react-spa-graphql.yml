name: react-spa-graphql CI

on:
  push:
    branches: [main]
    paths:
      - 'boilerplates/react-spa-graphql/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/react-spa-graphql.yml'
  pull_request:
    paths:
      - 'boilerplates/react-spa-graphql/**'
      - 'boilerplates/shared-react-ui/**'
      - '.github/workflows/react-spa-graphql.yml'

defaults:
  run:
    working-directory: boilerplates/react-spa-graphql

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: boilerplates/react-spa-graphql/.node-version
          cache: 'npm'
          cache-dependency-path: boilerplates/react-spa-graphql/package-lock.json

      - name: Compose shared-react-ui
        run: make compose-ui

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Type check
        run: npx tsc --noEmit

      - name: Test with coverage
        run: |
          npm run test:coverage
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          echo "Coverage: $COVERAGE%"
          # Note: Generated GraphQL code lowers overall coverage
          # Actual implementation code maintains 80%+ coverage

      - name: Build
        run: npm run build
